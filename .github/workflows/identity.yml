name: Identity CI/CD

on:
  push:
    paths:
      - 'backend/services/IdentityService/**'

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Set tag
        run: echo "IMAGE_TAG=$(date +%s)" >> $GITHUB_ENV

      - name: Build image
        run: docker build --platform=linux/amd64 -t easyshop-identity-service:${{ env.IMAGE_TAG }} ./backend/services/IdentityService

      - name: Load into Kind
        run: kind load docker-image easyshop-identity-service:${{ env.IMAGE_TAG }} --name easyshop-cluster

      - name: Deploy
        run: kubectl set image deployment/identity-deployment identity=easyshop-identity-service:${{ env.IMAGE_TAG }}